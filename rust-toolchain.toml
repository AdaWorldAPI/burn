[toolchain]
# Bump log (append-only — add a line here, don't rewrite prose elsewhere):
#   2026-09-05: 1.94.1 -> 1.98.1, joins the workspace-wide 1.98.1 sweep.
#   2026-09-05: re-added after the upstream resync dropped this file; the
#               sweep target is unchanged.
channel = "1.98.1"
components = ["clippy", "rustfmt"]
